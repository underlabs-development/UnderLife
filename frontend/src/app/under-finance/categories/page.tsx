import { CategoryManager } from "@/features/finance";

export default function UnderFinanceCategoriesPage() {
  return (
    <main className="relative min-h-screen bg-[var(--bg-void)]">
      <CategoryManager />
    </main>
  );
}
