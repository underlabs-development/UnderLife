import { TransactionReview } from "@/features/finance";

export default function UnderFinanceReviewPage() {
  return (
    <main className="relative min-h-screen bg-[var(--bg-void)]">
      <TransactionReview />
    </main>
  );
}
